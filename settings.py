"""
配置文件

修改 PROMPT_FORMAT 即可自定义提示词模板
占位符格式: ${列名}
"""

# ============================================
# 提示词模板配置（在此处修改你的模板）
# ============================================

PROMPT_FORMAT = "把名字变成大写，根据年龄计算出生年份：名字:${name}，年龄:${age}"
# PROMPT_FORMAT = "把名字变成大写：名字:${name}"
# PROMPT_FORMAT = "请介绍 ${name}，TA 今年 ${age} 岁"

# CSV 文件路径
CSV_FILE = "person.csv"

# API 配置（从 .env 读取）
# 这些会在 main.py 中使用

# ============================================
# 分类任务配置
# ============================================

# 任务类型: "binary" 为二分类，"multiclass" 为多类别分类
TASK_TYPE = "binary"

# ============================================
# 二分类配置 (TASK_TYPE = "binary" 时使用)
# ============================================

# 二分类标签映射
# label 1 代表"是"，label 0 代表"否"
BINARY_CLASS_CONFIG = {
    "positive_label": "是",  # 对应 label 1
    "negative_label": "否",  # 对应 label 0
}

# ============================================
# 多类别分类配置 (TASK_TYPE = "multiclass" 时使用)
# ============================================

# 多类别分类标签列表
# 示例: ["体育", "财经", "房产", "家居", "教育", "科技", "时尚", "时政", "游戏", "娱乐"]
# label 从 0 开始递增，按列表顺序分配
MULTICLASS_CONFIG = {
    "classes": [],  # 在此处填入所有类别名称
}

# ============================================
# LLM 结果解析配置
# ============================================

"""
以CSV格式返回，prompt和llm_answer列名固定，其他列名根据提示词模板中的占位符动态生成
"""
